# DropIn

Upload a video of a real place. Walk around inside it.

Built in one hour at the Tel Aviv Claude Code Fable 5.1 Build Day.

- `web/` — Vite + React + TypeScript + Three.js (the playable world)
- `server/` — FastAPI (upload, frames, Claude vision, job stages)
- `CONTRACT.md` — the API between them
